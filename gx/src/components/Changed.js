

const Changed = ({title, subTitle}) => {
    return (
      <header>
        <h1 className="fontColor">{title}</h1>
        <h3 className="fontColor">{subTitle}</h3>
      </header>
    );
  };


export default Changed;